# Discord Stream Monitor - Environment Configuration
# Copy this file to .env and fill in your actual values

# Your Discord user account token (NOT a bot token)
# To get your token (EASY METHOD):
# 1. Open Discord in browser (desktop app won't work)
# 2. Press F12 to open Developer Tools
# 3. Go to Network tab
# 4. In the Filter box, type: /api
# 5. Refresh the page (F5) - you should see fewer requests now
# 6. Click on ANY request (like "science" or "users/@me")
# 7. In the Headers section on the right, scroll down to "Request Headers"
# 8. Find "authorization:" and copy the FULL token
# 9. Paste it below (it's a very long string, that's normal!)
DISCORD_TOKEN=your_token_here
# Channel IDs to monitor (comma-separated, no spaces)
# To get channel IDs:
# 1. Enable Developer Mode in Discord (User Settings > Advanced > Developer Mode)
# 2. Right-click on a channel and select "Copy ID"
# Example: CHANNEL_IDS=123456789012345678,987654321098765432
CHANNEL_IDS=your_channel_id_here

# Timezone offset for daily log files (default: +07:00 for WIB)
# Format: +HH:MM or -HH:MM
TIMEZONE_OFFSET=+07:00
